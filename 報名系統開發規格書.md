# 客製化活動報名系統 — 開發規格書

> 本文件為完整開發需求，目標是交給 Claude Code（或工程師）直接依此建置一套可自行維運的報名系統。系統需支援多場次活動、條件式報名表單、審核與付款流程、報到 QR code、以及後台角色權限管理。

---

## 0. 專案背景與目標

過去活動報名使用 Google 表單 + Google 試算表人工審核，痛點如下：
- 分頁式表單造成填寫體感疲勞
- 綠界付款連結需人工逐筆產生
- 審核、分組、退費、重複報名比對皆為人工作業，耗費大量行政時間
- 試算表以「知道連結即可檢視」分享，導致民眾個資（身分證字號、戶籍地址、電話、Email）曝險
- 多場次活動需要人工在多份試算表間比對是否重複報名

新系統目標：**將報名、審核、繳費、通知、報到、公告全流程系統化，並用登入 + 角色權限取代連結式共用試算表。**

---

## 1. 技術棧

| 模組 | 選用 | 原因 |
|---|---|---|
| 前端 + 後端 | Next.js（App Router） | 前後端同一專案，Claude Code 熟悉 |
| 資料庫 / 檔案儲存 / 登入權限 | Supabase（Postgres + Storage + Auth + RLS） | 三合一，免自建伺服器，且原生支援 Row Level Security 做角色權限 |
| Email 發送 | Resend（或 SendGrid） | 交易型信件，API 簡單，需搭配背景佇列 |
| 付款 | 綠界 ECPay（線上刷卡/ATM）+ 人工轉帳登記 | 全自動 API 串接 + webhook 回調更新狀態 |
| OCR 文字辨識 | Google Cloud Vision 或 Azure Document Intelligence | 半自動輔助審核，非全自動判定 |
| QR code | `qrcode`（npm 套件） | 純程式產生，報到時掃碼比對資料庫 |
| 部署 | Vercel + Supabase | 免費額度足夠一般活動規模 |

---

## 2. 資料模型（核心概念）

### 2.1 階層關係

```
活動系列 (event_series)  例：114年防災教育日系列活動
  └─ 場次 (event_sessions)  例：和美場-主辦搭設帳、和美場-民眾自搭帳、花壇場...
       └─ 報名 (registrations)  一筆 = 一組（含聯絡人 + 最多N名成員，人數上限可自訂）
            └─ 報名成員 (registration_members)  含聯絡人本人
```

- **活動系列**：一年一屆的整體活動，未來開新一屆可「複製設定」快速建立。
- **場次**：同系列下的個別報名單位，各自有獨立的表單欄位設定、身分別選項、負責審核單位、名額、費用規則。**重複報名比對的範圍＝同一活動系列內跨所有場次**（符合過去「每人限報一場」的規則）。
- **每筆報名可自訂人數上限**（後台設定，可以是1人一筆，也可以是4人一筆），非寫死。

### 2.2 主要資料表（欄位僅列重點，實作時再補齊型別/約束）

**event_series**
`id, name, year, status(draft/open/closed/archived), created_by`

**event_sessions**
`id, series_id, name(如"和美場-主辦搭設帳"), location, date_start, date_end, capacity_total, registration_open_at, registration_close_at, result_announce_at, cancel_deadline_at, fee_base, fee_discount_per_person, max_members_per_registration, managing_org(負責審核的公所/單位), status`

**session_identity_types**（身分別設定，隨場次自訂）
`id, session_id, name(工作人員/民眾), requires_org_field(bool), org_options(json陣列：公所/消防局/社會處/衛生局/警察局/社區/民間團體...)`

**session_fee_categories**（免付費/減免資格分類，隨場次自訂）
`id, session_id, code(如"(一)"), label, discount_amount, required_document_type, applies_to(免付費/減免)`

**registrations**（一筆＝一組）
`id, session_id, contact_email, contact_phone, submitted_at, review_status(審核中/審核通過/退回補件), admission_status(未抽籤/正取/備取/取消), waitlist_rank(備取順位), payment_status(無需繳費/待繳費/已完成), payment_amount, payment_method(online/manual), ecpay_trade_no, ecpay_link, manual_transfer_last5, manual_transfer_note, group_zone(分組區域如"B區"), group_number(帳篷編號), tent_type, sleeping_bag_own_qty, sleeping_bag_rent_qty, is_cancelled(bool), cancelled_at, cancel_reason, refund_amount, refund_status, duplicate_flag(bool), duplicate_note(text), checkin_at(報到時間), checkin_by, qr_token, admin_note, created_at, updated_at`

**registration_members**（報名成員，含聯絡人本人 member_order=0）
`id, registration_id, member_order, name, id_number(加密儲存), household_address, birth_year_roc, gender, identity_type_id, org_selected, org_other_text, fee_category_id, fee_review_result(審核中/審核通過/需繳費/無需繳費), ocr_extracted_text(json), created_at`

**registration_files**
`id, registration_id, member_id(nullable), file_type(身分證/戶籍謄本/健保卡/身心障礙證明...), storage_path, uploaded_at, ocr_status, ocr_result(json)`

**admin_users**
`id, email, name, role(vendor/host_org/finance), managed_session_ids(該角色可管哪些場次)`

**email_logs**
`id, registration_id, type(審核結果/付款通知/場次資訊/報到QR/遞補通知), status(pending/sent/failed), sent_at, error_message`

**duplicate_matches**
`id, registration_id_a, registration_id_b, matched_by(id_number+name / name+phone), diff_summary(json), resolved(bool), resolved_note`

---

## 3. 前台：報名表單

### 3.1 UX 原則（取代舊版分頁式表單）

- **單頁 + 動態展開**，不做分頁跳轉：選擇身分別後，對應欄位在同一頁往下展開。
- 進度以不強調頁數的視覺提示呈現（例如已完成小圓點），避免「還有幾頁」的心理壓力。
- 支援草稿暫存（本機或以 Email 驗證後續填），避免中途關閉需重填。
- 送出前才統一檢查必填項，逐一展開時不強制中斷。

### 3.2 條件式必填欄位邏輯

- 依「身分別」（工作人員／民眾）動態顯示：
  - 工作人員 → 顯示「所屬單位」單選（公所/消防局/社會處/衛生局/警察局/社區/民間團體/其他），選「民間團體」再展開「團體全名」文字欄
  - 民眾 → 不顯示上述欄位
- 依「是否申請免付費/減免資格」動態顯示對應「應檢附證明文件」提示與上傳欄位
- 每筆報名可有多位成員（人數上限依場次設定），每新增一位成員即動態展開一組相同的個人資料欄位

### 3.3 檔案上傳

- 支援多檔上傳（身分證、戶籍謄本、健保卡、身心障礙證明等），可依場次設定限制檔案格式與大小
- 上傳後呼叫 OCR 服務辨識文字，將辨識出的關鍵資訊（身分證字號、地址關鍵字如「花壇鄉」）**預先帶入審核畫面**並標色提示「系統判斷符合資格」，**最終仍需審核人員按鍵確認**，不做全自動判定（財務風險 + 個資風險考量）

### 3.4 送出前確認

- 顯示活動辦法/注意事項全文，需勾選「已詳讀並同意」才能送出（單選必填題）
- 個資蒐集告知事項同意書（另一必填同意題）

---

## 4. 審核與付款流程

### 4.1 審核

- 後台顯示待審核列表，逐筆或逐人審核（每位成員的免付費資格可分別審核，狀態：審核中/審核通過/需繳費/無需繳費）
- OCR 預先帶入的判斷僅供參考，審核人員按鍵確認後才寫入正式狀態
- 審核通過後系統自動組成逐人結果描述文字（如「OOO，符合免付費申請資格，無需繳交報名費」），供後續通知信件使用

### 4.2 錄取結果與付款通知（人工觸發、系統批次執行）

1. 主辦完成所有審核 + 錄取/備取判定後，後台提供「寄出審核結果通知」按鈕
2. 點擊後跳出確認彈窗（「確定要寄出通知給 N 位錄取者、M 位備取者？」），確認後才執行
3. 系統批次寄送（走 Email 佇列，逐筆記錄 email_logs 狀態，避免大量同時觸發卡住或觸發第三方速率限制）
4. 需繳費者的通知信附上繳費方式：
   - 線上：系統呼叫綠界 API 自動產生付款連結（不再人工逐筆貼連結）
   - 人工轉帳：信件顯示轉帳帳號，請回報後五碼

### 4.3 付款狀態（三態）

`無需繳費` / `待繳費` / `已完成`

- 線上付款：綠界 webhook 回調自動將狀態改為「已完成」
- 人工轉帳：民眾回報後五碼後，財務角色人工核對並手動改狀態（特殊情況才用此流程）

---

## 5. 分組與活動前通知

- 分組（帳篷區、帳篷編號等）由主辦人工於後台指派，欄位需可自訂（不同活動可能是帳篷分組，也可能是別種分組邏輯）
- 活動前，後台提供「依身分別批次寄送活動通知」功能：
  - 依身分別（工作人員/民眾等）帶入不同信件內容
  - 內容包含：組別資訊、參加人數、需自備物品清單、報到用 QR code、其他提醒事項
  - 同樣走 Email 佇列批次發送並記錄寄送狀態（對應後台「收信回報」欄位）

---

## 6. 報到系統

- 獨立網頁（僅限主辦/工作人員登入使用，不對外開放），供活動現場用手機開啟
- 開啟相機掃描民眾的 QR code
- 掃描成功後：
  - 自動記錄報到時間、操作的工作人員帳號
  - 畫面顯示該筆報名的帳篷編號、租借物資（睡袋/睡墊數量）、人數等資訊，供現場快速核對，不需翻紙本名單
- QR code 內容為加密 token（`qr_token`），避免直接暴露報名 ID 或個資

---

## 7. 後台管理系統

### 7.1 角色與權限矩陣

| 角色 | 可編輯 | 唯讀 |
|---|---|---|
| 廠商 | 全部（含系統設定、表單欄位建置、活動系列/場次建置） | — |
| 主辦機關/公所 | 審核結果、分組、錄取結果、取消狀態 | 繳費情況、其餘欄位 |
| 財務 | 繳費情況（含人工核對轉帳） | 其餘欄位 |

- 權限依「場次」分派：不同公所只能看到/編輯自己負責場次的資料，看不到其他場次
- 用 Supabase RLS（Row Level Security）在資料庫層級強制隔離，不只是前端隱藏

### 7.2 審核表主要欄位（對照過去 Excel 審核表整理）

每筆報名列出：報名時間、審核結果(審核中/退回補件)、錄取結果(正取/備取/取消)、分組(區/編號)、繳費情況、繳費金額、付款連結、聯絡人資訊、每位成員資訊(姓名/證號/地址/出生年/性別/免付費申請與審核結果/所屬單位/證明文件連結)、備註、睡袋墊自備/租借數量、重複報名標記、退費金額(取消時自動試算)

### 7.3 增設欄位（依你的需求新增）

- `取消`：獨立欄位（不併入審核結果），含取消時間、取消原因，並連動退費計算
- `審核結果`：獨立於「錄取結果」，代表資格審查的結果
- `付費結果`：三態
- `收信回報`：每封信的寄送狀態（成功/失敗/未讀）
- `退費計算`：依取消時間點自動試算，人員可覆核調整

---

## 8. 重複報名比對

- 比對範圍：**同一活動系列內，跨所有場次**
- 比對依據：`身分證字號 + 姓名` 或 `姓名 + 電話` 命中即視為疑似重複
- 命中後：
  - 在兩筆報名資料上都標記 `duplicate_flag = true`
  - 產生 `duplicate_matches` 紀錄，列出兩筆資料的差異欄位（供人工快速比對，不用整份對照）
  - 後台跳出提示，由人員聯繫當事人確認後，人工決定保留哪一筆、並在 `duplicate_matches.resolved_note` 記錄處理結果

---

## 9. 退費計算

- 規則：依「取消通知時間點」判斷
  - 期限前通知取消 → 全額退費（需扣除轉帳手續費）
  - 逾期 → 不予退費
- `refund_amount` 由系統依 `is_cancelled` 時間與場次設定的 `cancel_deadline_at` 自動試算，人員可於後台調整覆核

---

## 10. 備取遞補

- 有人取消名額釋出時，系統從備取名單依 `waitlist_rank` 順位自動觸發：
  1. 系統寄出遞補通知信，附回覆期限
  2. 同時後台顯示「待電話確認」提示給主辦
  3. 主辦電話聯繫後，於後台按下「已電話確認」→ 立即停止該筆的系統期限倒數計時，避免電話已確認但系統又因期限到而重複遞補下一位
  4. 若期限到期仍未任何一方確認，系統自動遞補下一順位

---

## 11. 結果公告（打碼公開）

- 報名結果公告時，於原報名頁面公開（含正取與備取），**此時關閉報名填寫功能**
- 公開資料打碼規則：
  - 姓名：僅顯示姓氏+O+名字最後一字（如「王O明」），組內全員皆打碼
  - 顯示：組別編號、免付費審核情況、繳費情況（**即時更新**，非公告當下的快照）
  - 備取名單：顯示備取順位

---

## 12. Email 通知總覽

| 情境 | 觸發方式 | 內容 |
|---|---|---|
| 審核結果通知（含付款通知） | 主辦手動按鈕批次觸發 | 錄取/備取結果、免付費逐人結果描述、需繳費者附繳費方式 |
| 遞補通知 | 系統自動（名額釋出時） | 遞補資格、回覆期限 |
| 活動前通知 | 主辦手動按鈕，依身分別批次觸發 | 組別、人數、需自備物品、報到QR code |
| 重複報名提醒 | 系統偵測後台顯示，不寄信給民眾 | — |

所有信件皆走背景佇列發送（非同步），逐筆記錄 `email_logs`，避免大量觸發卡住或觸發第三方 API 速率限制。

---

## 13. 安全與隱私要求

- 後台一律登入制（Supabase Auth）+ 角色權限（RLS），**禁止**用「知道連結即可檢視」的方式分享任何含個資的頁面或檔案
- 身分證字號等敏感欄位加密儲存，存取需記錄操作紀錄（誰在何時查看/修改）
- 上傳證件檔案儲存於權限受控的 Storage bucket，非公開存取
- 打碼公告頁為唯一對外公開頁面，且只顯示打碼後資料
- 活動結束後個資保存期限與銷毀規則，建議由主辦機關依內部規範另訂，系統需支援批次匯出後刪除

---

## 14. 非功能需求

- 規模：需支援單場活動 500+ 人同時報名，及多場次活動並行
- Email 發送走佇列，避免同步阻塞
- 資料庫查詢需對「同系列跨場次重複比對」「依場次篩選權限」建立適當索引
- 活動系列/場次設定可「複製既有設定」快速建立新一屆活動，降低每年重新建置成本

---

## 15. 建議開發順序（給 Claude Code 的執行順序建議）

1. 資料庫 schema 與 RLS 權限規則
2. 後台：活動系列/場次/表單欄位建置（廠商角色）
3. 前台：動態報名表單 + 檔案上傳
4. 後台：審核介面（含 OCR 預帶入輔助）
5. Email 佇列 + 審核結果/付款通知批次寄送
6. 綠界金流串接（API + webhook）
7. 分組介面 + 活動前通知批次寄送
8. 報到 QR code 產生與掃碼頁面
9. 重複報名比對邏輯
10. 退費計算 + 備取遞補機制
11. 結果打碼公告頁面
12. 安全性檢查（RLS 測試、敏感欄位加密驗證）

---

## 附錄：狀態機摘要

**審核結果（每位成員）**：審核中 → 審核通過 / 需繳費 / 無需繳費

**錄取結果（每筆報名）**：待抽籤 → 正取 / 備取 → （備取可遞補為正取）→ 可被標記為取消

**繳費狀態**：無需繳費 / 待繳費 → 已完成

**取消與退費**：未取消 → 已取消（記錄取消時間）→ 依時間點試算退費金額 → 退費狀態（待退/已退）
