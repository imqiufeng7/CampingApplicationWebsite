# 客製化活動報名系統 — 功能與架構規格書 v3（依實作現況整理）

> 本文件是 v2 之後再依實作進度更新的版本，聚焦「功能、架構邏輯、使用需求」，不含外觀/視覺設計細節。相較 v2，本版新增「後台儀表板」一節（第 7 節）。

---

## 0. 系統目標

以「登入 + 角色權限」取代 Google 表單 + 試算表的人工報名流程，將報名、審核、繳費、通知、修改、公告全流程系統化，並在資料庫層（而非僅前端）強制角色權限與個資保護。

---

## 1. 角色與權限

| 角色 | 可編輯範圍 | 說明 |
|---|---|---|
| vendor（廠商） | 全部欄位、場次建置、系統設定 | 唯一能建立/修改場次結構、管理其他管理員帳號的角色 |
| host_org（主辦機關/公所） | 審核結果、錄取結果、分組、睡袋數量、取消狀態與原因、退費金額/狀態、重複標記、備註、每位成員的身分別/單位/減免審核結果 | 依「場次」分派權限，只能看/改自己負責的場次 |
| finance（財務） | 繳費狀態、金額、方式、綠界交易資訊、人工轉帳後五碼與備註 | 同樣依場次分派 |

- 三種角色皆可「讀取」所有欄位；只有「編輯」權限依角色分開。
- **權限矩陣在資料庫層強制執行**（不是只有前端隱藏欄位）：即使繞過前端介面直接呼叫 API，資料庫也會拒絕不符權限的欄位異動。
- 未登入的一般民眾完全無法讀取任何報名資料（不存在「知道連結即可查看」的分享模式）。

---

## 2. 資料階層

```
活動系列 event_series
  └─ 場次 event_sessions
       └─ 報名 registrations（一筆＝一組，含聯絡人＋最多N名成員）
            └─ 報名成員 registration_members（含聯絡人本人）
                 └─ 上傳文件 registration_files
```

- 場次可設定：名稱、地點、日期、總名額（選填）、每筆報名人數上限、開放/截止報名時間、結果公告時間、取消退費期限、每人報名費。
- 每個場次可自訂多組「身分別」與「免付費/減免類別」。
- 場次可「複製既有場次設定」快速建立（含身分別、減免類別、所有文字內容），只需再調整名稱與開放時間。

---

## 3. 場次內容欄位（後台可編輯，皆支援富文本＋插圖排版）

| 欄位 | 用途 | 顯示位置/時機 |
|---|---|---|
| 同意頁面文字 | 送出前的條款同意文字，支援 `{{活動名稱}}` 動態代入系列+場次名稱 | 民眾進入表單看到的第一個畫面（同意/不同意閘門） |
| 開頭介紹 | 活動簡介 | 同意頁與表單頁最上方 |
| 流程表 | 活動流程說明 | 同意頁與表單頁 |
| 報名流程說明 | 報名相關規則說明 | 同上 |
| 減免內容及應附文件說明 | 減免資格與應附文件說明 | 同上 |
| 活動辦法/注意事項 | 正式活動辦法全文（必填） | 同意頁 + 表單最終同意勾選處 |
| 個人資料提供同意書全文 | PII 蒐集告知事項 | 表單最終同意勾選處 |
| 送出前提醒事項 | 例如公布時間、通知方式、取消異動期限與聯絡方式、退費規則、遞補說明 | 送出按鈕之前 |
| 送出成功後顯示文字 | 客製化感謝文字 | 送出成功畫面 |
| 送出成功後導向按鈕 | 網址＋按鈕文字 | 送出成功畫面（選填） |
| 活動視覺 banner | 圖片 | 表單最上方 |
| 報名頁面背景顏色 | 選填開關＋顏色 | 整個報名頁面 |
| 地點 → 自動嵌入地圖 | 用地點文字自動產生 Google Maps 嵌入畫面 | 表單頁 |

未填寫的欄位，前台不顯示對應區塊（不會出現空白區塊）。

---

## 4. 前台報名流程

1. **進入檢查**：場次是否為開放中狀態、是否在報名時間窗口內、是否已達名額上限（額滿則直接關閉填寫，顯示已滿訊息）。
2. **資訊呈現**（依序）：banner → 標題/地點 → 地圖 → 開頭介紹 → 流程表 → 報名流程說明 → 減免內容及應附文件 → 活動辦法/注意事項。
3. **同意閘門**：顯示可自訂的同意條款文字，需點擊「同意」才會出現實際填寫欄位；「不同意」則無法繼續報名。
4. **填寫表單**（單頁動態展開，非分頁跳轉）：
   - 聯絡資訊：Email、電話（必填）。
   - 每位成員（上限依場次設定，可「新增成員」）：
     - 姓名、身分證字號（必填）
     - 出生日期：民國年（20–115）／月／日，以**滾動轉盤**方式選擇（非文字輸入，避免格式錯誤）
     - 生理性別：男／女／跨性別
     - 戶籍地址
     - 是否申請免付費/減免資格（選填；選擇後若該類別有應附文件，動態展開上傳欄位並列為必填）
     - 「我是工作人員」勾選框（**僅在場次設定了需要單位欄位的身分別時才出現**；勾選後才展開「所屬單位」，選「民間團體」再展開「團體全名」欄位）
   - 注意事項與同意書：活動辦法同意、個資同意（兩者皆為必填勾選）
   - 送出前提醒事項（顯示提醒文字，非需勾選確認）
5. **送出**：以單一資料庫交易原子性寫入報名主檔＋所有成員＋檔案關聯，並回傳**規範化流水編號**（格式 `R000123`），同時再次驗證名額與報名時間窗口（避免併發搶最後名額）。
6. **送出成功畫面**：
   - 顯示可自訂的成功文字＋報名編號
   - 系統自動寄送確認信（含資料摘要與專屬修改連結）；寄信失敗不影響報名本身已成功
   - 提供「重新寄送副本到我的 Email」按鈕（有次數上限防濫用）
   - 若場次有設定導向連結，顯示導向按鈕（例如前往主辦方官網）

---

## 5. 報名後修改機制

- 每筆報名擁有一組獨立、不可預測的秘密權杖（edit token），**不使用「姓名＋身分證字號」這類容易被冒用的驗證方式**。
- 確認信中附上專屬修改連結（`/edit/[token]`）。
- 透過此連結可修改：聯絡資訊、每位成員的所有個人資料欄位、重新上傳/覆蓋證明文件。
- 不可透過此連結：新增或刪除成員、變更所屬場次。
- 已標記取消的報名無法再透過此連結修改。

---

## 6. 後台審核介面

### 審核列表
顯示：報名編號、報名時間、聯絡人、成員名單（含人數）、審核結果、錄取結果（含備取順位）、分組資訊、睡袋自備/租借數量、繳費情況與金額、備註、狀態標籤（已取消／疑似重複）。

**排序規則**：
1. 能以現有人數自備足夠睡袋/睡墊的組別優先
2. 其次依報名人數由多到少排序
3. 同一排序層級內，依報名時間由早到晚排序

### 逐筆審核頁
- 「上一筆／下一筆」快速切換（依審核列表相同排序，不需返回列表重新選取）
- 每位成員：顯示解密後身分證字號（僅授權審核者可見）、身分別/所屬單位、申請的減免類別、逐人減免審核結果（下拉即時更新）、已上傳證明文件的檢視連結
- 審核／錄取／分組／取消區塊
- 繳費資訊區塊（見第 8 節自動計算規則）
- 通知寄送紀錄：可展開查看每封信實際寄出的主旨與內文（不需要看伺服器 log）

---

## 7. 後台儀表板（新增）

**目的**：讓三種角色一進後台就能掌握每個場次「目前最新情況」，不需要逐一點開審核/繳費列表才看得到彙總數字。

**存取範圍**：與其他後台頁面相同的場次權限規則 — vendor 看得到全部場次；host_org / finance 只看得到自己被指派的場次。

**每個場次一張卡片，顯示**：

| 區塊 | 內容 |
|---|---|
| 場次資訊 | 系列名稱＋場次名稱、場次狀態（開放中／已關閉等） |
| 報名組數 | 有效報名組數（不含已取消）、已取消組數 |
| 總人數 | 目前有效報名的總人數 |
| 名額使用狀況 | 若場次有設定總名額 → 顯示「目前人數／總名額」；未設定則顯示「未設定」 |
| 審核結果分布 | 依審核結果（審核中／審核通過／退回補件等）分別列出組數 |
| 錄取結果分布 | 依錄取結果（未抽籤／正取／備取／取消）分別列出組數 |
| 繳費情況分布 | 依繳費狀態（無需繳費／待繳費／已完成）分別列出組數，並加總「已收金額」與「待收金額」 |
| 快速連結 | 直接跳到該場次的「審核」與「繳費」列表頁 |

**計算邏輯**：即時從 `registrations`／`registration_members` 現有資料彙總計算（沿用審核/繳費列表頁同樣的欄位），不額外建立彙總表或快照，因此永遠反映當下最新狀態。

---

## 8. 繳費金額/狀態自動推導規則（核心業務邏輯）

**觸發時機**：任一成員的「減免審核結果」發生異動時，系統自動重新計算該筆報名的繳費資訊。

**判斷規則**（依成員逐一判斷）：
- 減免審核結果為「**無需繳費**」或「**審核通過**」→ 視為該成員**不需繳費**（審核通過＝減免資格核准，等同無需繳費）
- 減免審核結果為「**審核中**」或「**需繳費**」→ 視為該成員**需要繳費**

**整筆報名的計算**：
- 若所有成員皆不需繳費 → 繳費狀態自動設為「無需繳費」，金額 = 0，繳費方式清空
- 若有任何成員需要繳費 → 繳費狀態自動設為「待繳費」，**金額 = 需繳費人數 × 場次設定的「每人報名費」**，繳費方式預設為「線上刷卡」（若尚未設定過繳費方式）
- **例外**：一旦繳費狀態已經是「已完成」，後續審核異動**不會**再自動覆蓋（避免已收款資料被意外改動），此時僅財務角色可手動調整

財務角色仍可人工覆核：切換為人工轉帳、記錄轉帳後五碼、手動確認「已完成」（線上刷卡則由金流 webhook 自動確認完成）。

---

## 9. 報名額滿自動關閉

- 場次可設定總名額（選填欄位）。
- 若已設定，且目前非取消狀態的報名筆數達到上限，前台自動關閉填寫並顯示「名額已滿」訊息。
- 資料庫寫入層同時做二次防呆（鎖定場次列後才驗證名額），避免兩人同時搶最後名額造成超收。

---

## 10. Email 通知

| 情境 | 觸發方式 | 內容 |
|---|---|---|
| 報名確認信 | 系統自動（送出報名當下） | 報名編號、成員名單、專屬修改連結 |
| 審核結果通知 | 主辦手動觸發批次寄送（二次確認彈窗顯示錄取/備取人數） | 逐人審核結果描述、需繳費者附繳費方式（線上連結或人工轉帳資訊） |

- 所有寄送記錄**實際的主旨與內文**皆存入資料庫，後台可直接展開查看，不需要看伺服器 console。
- 寄信服務採可替換介面設計：目前尚未串接正式 Email 服務商，先以「記錄不真的寄出」方式運作；之後接上正式服務商時不需更動其他程式邏輯。

---

## 11. 金流

- **綠界 ECPay 線上金流**：系統自動產生付款連結（不需人工逐筆產生），綠界 webhook 回調自動將繳費狀態更新為「已完成」。
- **人工轉帳**：備用方式，供無法線上付款者使用，民眾回報帳號後五碼，財務人工核對後手動確認。

---

## 12. 資料安全

- 後台一律登入制＋角色權限，資料庫層（非僅前端）強制隔離不同角色與不同場次的存取範圍。
- 身分證字號加密儲存，僅授權審核角色可透過專用機制解密查看，一般查詢無法取得明文。
- 上傳證件私有儲存，僅授權者可取得限時的簽名下載連結，無法用固定網址直接存取。
- 前台報名連結、修改連結皆不包含可推測的識別資訊，任何頁面都不會意外洩漏他人報名資料。

---

## 13. 尚未實作／延後項目

以下功能的資料欄位已預留（不需破壞性資料庫變更即可日後擴充），但邏輯尚未實作：

- OCR 輔助審核（上傳證件自動預帶身分證字號等資訊）
- QR code 現場報到（掃碼比對＋報到記錄）
- 同一活動系列跨場次的重複報名比對
- 退費金額自動試算（目前為人工填寫，可人工覆核調整）
- 備取名額自動遞補通知
- 結果打碼公開頁面（正取/備取名單公開，姓名打碼）
- 活動前依身分別批次寄送提醒通知（組別、需自備物品、報到 QR 等）
